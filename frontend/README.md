# SYNTHIA | Research Dashboard

This contains the React frontend for the SYNTHIA research dashboard.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
3. Build for production:
   `npm run build`
